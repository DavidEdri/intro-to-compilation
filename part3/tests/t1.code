    function bool fee2()
    {
        var int i, l, m;
        var bool x, j; /* declarations */
        var char k;
        k = '@'; /* statements */
        i = l + l;
        if ((k == '*') || (x != false) && (l + m < i)){
            x = l < m;
        }
        return x;
    }

    function void fee1(int i, j, k, x)
{
    {
	var char x;
	var bool k;
	k = fee2() || false;
     }
    x = k;
}

function void main(){

}